spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/task-db-name
    username: task-db-user
    password:  task-db-password
  jpa:
    properties:
      hibernate:
        format_sql: true
